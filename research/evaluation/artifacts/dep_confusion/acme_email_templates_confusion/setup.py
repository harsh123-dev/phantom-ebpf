from setuptools import setup
setup(
    name="acme-email-templates",
    version="9.9.9",
    packages=["acme_email_templates"],
    description="PHANTOM evaluation dependency confusion beacon (safe research artefact)",
)
